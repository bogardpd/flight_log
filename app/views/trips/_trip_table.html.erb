<table class="flightlog">
  <tr>
    <th class="trip_name">Trip</th>
    <th class="flight_date">Departure</th>
  </tr>
	<%= render :partial => "trips/trip_table_body" %>
  <tr><td colspan="2" class="flightlog_total"><%= pluralize(@trips.length, 'trip') %></td></tr>
</table>