<% linkable = true if local_assigns[:linkable].nil? %>
<td class="flight-route<%= @top_row_style %>">
	<%= (!linkable || (params[:controller] == "airports" && params[:action] == "show" && @airport == flight.origin_airport)) ? code_mono(flight.origin_airport.iata_code) : link_to(code_mono(flight.origin_airport.iata_code), airport_path(flight.origin_airport.iata_code), :title => "View airport details for " + flight.origin_airport.city) %> <%= Route::ARROW_ONE_WAY_HTML %> <%= (!linkable || (params[:controller] == "airports" && params[:action] == "show" && @airport == flight.destination_airport)) ? code_mono(flight.destination_airport.iata_code) : link_to(code_mono(flight.destination_airport.iata_code), airport_path(flight.destination_airport.iata_code), :title => "View airport details for " + flight.destination_airport.city) %>
</td>