<h1>Aircraft Families</h1>

<p>This listing shows how often I've flown on each particular type (family) of airplane. Each type of airplane may have several variants, but all of those variants have been included in each family's total.</p>

<p>I have flown on <%= pluralize(@aircraft_array.count, 'aircraft family') %>.</p>

<table class="flightlog">
  <tr>
    <th class="aircraft_family">Aircraft Family</th>
    <th class="aircraft_flights">Flights</th>
  </tr>
<% @aircraft_array.each do |aircraft| %>
  <tr>
    <td class="aircraft_family graph" style="background-size: <%= (aircraft[:count]*300/@aircraft_maximum) %>px 100%;"><%= link_to(aircraft[:aircraft], show_aircraft_path(aircraft[:aircraft].gsub(" ", "_")), :title => "View flights on #{aircraft[:aircraft]} aircraft") %></td>
    <td class="aircraft_flights"><%= aircraft[:count] %></td>
  </tr>
<% end %>
  <tr><td colspan="2" class="flightlog_total"><%= pluralize(@aircraft_array.count, 'aircraft family') %></td></tr>
  
</table>

