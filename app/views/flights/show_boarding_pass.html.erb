<% provide(:title, "Boarding Pass Results") %>
<% provide(:meta_description, "Results from the boarding pass barcode parser.") %>
<% add_breadcrumb "Boarding Pass", boarding_pass_path %>
<% add_breadcrumb @results, boarding_pass_path %>

<h1>Boarding Pass Results</h1>

<div id="bcbp-input-form">
  <%= form_tag(build_boarding_pass_path, method: :get, id: "bcbp-form") do %>
  <p>
    <%= label :data, "Update your boarding pass data here:" %><br />
	  <%= text_area_tag :data, params[:data], size: "70x5", class: "code-mono" %>
  </p>
  <p><%= submit_tag "Update", name: nil, id: "bcbp_submit_button" %></p>
  
<% end %>
</div>

<% if @boarding_pass.data.present? %>
  <%= render "bcbp", boarding_pass: @boarding_pass %>
<% else %>
  <p>Weʼre sorry, but we were unable to parse the boarding pass data provided.</p>
<% end %>