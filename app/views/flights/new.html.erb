<h1>New Flight</h1>

<%= form_for(@flight) do |f| %>

  <%= render 'error_messages' %>
  
  <%= render :partial => 'form_fields', :locals => { :f => f } %>
  
  <p>
    <%= f.submit "Add Flight" %>
  </p>

<% end %>