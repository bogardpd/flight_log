<% linkable = true if local_assigns[:linkable].nil? %>
<% cell_html = airline_icon(flight.airline.icao_airline_code, title: "View flight details for #{flight.airline.airline_name} #{flight.flight_number.to_s}") + "#{flight.airline.airline_name} #{flight.flight_number.to_s}" %>
<td class="flight-flight<%= @top_row_style %>"><%= content_tag(:div, "Hidden", class: "hidden-marker") if flight.trip.hidden %><%= linkable ? link_to(cell_html, flight_path(flight), title: "View flight details for #{flight.airline.airline_name} #{flight.flight_number.to_s}") : cell_html %></td>