<td class="flight_flight<%= " hidden_trip" if flight.trip.hidden %><%= @top_row_style %>"><%= "<div class=\"hidden_marker\">Hidden</div>".html_safe if flight.trip.hidden %><%= link_to(image_tag(airline_icon_path(flight.airline.iata_airline_code), :title => flight.airline.airline_name, :class => 'airline_icon') + flight.airline.airline_name + " " + flight.flight_number.to_s, flight_path(flight), :title => "View flight details for " + flight.airline.airline_name + " " + flight.flight_number.to_s) %></td>